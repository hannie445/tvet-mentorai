# Local secrets for `wrangler dev` / `npm run cf:preview`.
# Copy this file to .dev.vars and fill in your own values.
# Never commit .dev.vars - it's already in .gitignore.
#
# For production, set these with:
#   npx wrangler secret put OPENAI_API_KEY

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
